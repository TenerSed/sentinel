---
phase: 04-curated
plan: "02"
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/App.tsx
autonomous: true
requirements:
  - CUR-02
  - CUR-03
  - CUR-04
must_haves:
  truths:
    - "Only an explicit Recent-row click that remains selected for 10 seconds produces one `recent` Curated signal; initial selection, tab changes, coverage changes, re-renders, and unmounts cannot qualify an open."
    - "A `citation` Curated signal is appended only after the current record's native clipboard write succeeds; failed/missing clipboard access records no signal."
    - "Every submitted Chat question contributes only normalized known civic topic tags and a timestamp to Curated state before bundled/live/refusal routing; the raw text remains solely in existing Chat history and is never copied to Curated storage."
    - "Signals and manual topics survive browser refresh locally, remain separated from `lamplighter-chat-v1`, and selected coverage continues to determine the only candidate array used for ranking."
  artifacts:
    - path: src/App.tsx
      provides: "Isolated local Curated-state lifecycle and exact Recent/copy/Chat signal wiring through the Plan 1 helper"
      contains: "lamplighter-curated-v1"
  key_links:
    - from: "Recent row explicit onClick"
      to: src/curated.ts
      via: "starts one cancellable ten-second qualification timer before recording a recent signal"
      pattern: "recent"
    - from: "detail-panel Copy citation"
      to: src/curated.ts
      via: "clipboard promise resolution appends a citation signal for selected.id"
      pattern: "citation"
    - from: submitQuestion
      to: src/curated.ts
      via: "transient submitted text is converted to known topic tags only"
      pattern: "topicsFromQuestion"
---

<objective>
Capture the three approved personalization signals through existing interactions without changing evidence, Chat, or server behavior.

Purpose: make Curated respond to real local activity while retaining only the minimum allowed data.
Output: one isolated local profile updated by qualified Recent opens, successful citation copies, and submitted Chat topic tags.
</objective>

<execution_context>
@/Users/ssongirk/.codex/gsd-core/workflows/execute-plan.md
@/Users/ssongirk/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-curated/04-CONTEXT.md
@.planning/phases/04-curated/04-RESEARCH.md
@.planning/phases/04-curated/04-PATTERNS.md
@.planning/phases/04-curated/04-01-PLAN.md
@.planning/phases/04-curated/04-01-SUMMARY.md
@src/App.tsx
@src/curated.ts
@src/types.ts
@src/demo-seed.ts
@scripts/curated-check.mjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add an independent fail-closed Curated state lifecycle in the existing App shell</name>
  <files>src/App.tsx</files>
  <read_first>src/App.tsx; src/curated.ts; src/types.ts; .planning/phases/04-curated/04-CONTEXT.md; .planning/phases/04-curated/04-PATTERNS.md</read_first>
  <action>Reuse the guarded Chat `localStorage` lifecycle pattern, but create separate in-memory Curated state and a distinct `lamplighter-curated-v1` key supplied by the helper. On a ready seed, parse only via the Plan 1 fail-closed parser with current seed record IDs; malformed storage becomes an empty profile. Persist only that validated Curated shape after hydration. Keep all Chat keys/threads and their raw display history unchanged. Create one small state updater that calls the helper to append a signal and handles storage failures as optional-demo persistence failures, not as a reason to fetch or expose diagnostics. Derive ranked Curated data only from the existing `records = coverageRecords(state.seed, coverageId)` array and the helper; do not modify coverage calculation, server requests, packet construction, or seed data in App.</action>
  <verify><automated>npm run validate:curated && npm run validate:chat && npm run build</automated></verify>
  <acceptance_criteria>Curated state is isolated from Chat state and every later interaction has one safe local signal-write path.</acceptance_criteria>
  <done>The app owns a hydrated local Curated profile without adding any API, database, or provider dependency.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Capture qualifying Recent, copy, and Chat signals at their actual trust points</name>
  <files>src/App.tsx</files>
  <read_first>src/App.tsx; src/curated.ts; .planning/phases/04-curated/04-CONTEXT.md; .planning/phases/04-curated/04-PATTERNS.md</read_first>
  <behavior>
    - The first `selected` fallback record is display-only and never counts as a Recent open.
    - Clicking a Recent row starts/replaces a single 10,000ms timer for that record; it appends at most one qualified `recent` signal when it completes.
    - The timer is cancelled on selected-record change, coverage change, tab change away from Recent, unmount, and normal effect cleanup.
    - A successful clipboard promise appends a `citation` signal using the currently selected public record ID; rejection/no clipboard retains the existing error and records nothing.
    - `submitQuestion()` extracts tags from its transient trimmed text before all candidate-position, preset, unavailable, or live branches, and appends only a `chat` signal with tags/timestamp when tags exist.
  </behavior>
  <action>Wire the signal updater into the current App seams rather than creating event tracking components. Change the Recent row click handler to mark an explicit Recent selection and start a cancellable ten-second effect/timer keyed by record ID; do not start it for Chat/Curated citation selection or implicit `records[0]`. Ensure React cleanup prevents duplicate signal writes and clear pending qualification on coverage/tab selection changes. Refactor the existing Copy citation action to use the clipboard promise: after a successful `writeText(citation)` resolve, append the selected record's citation signal; keep the current copy-error message on unavailable/rejected clipboard access and never count it. At the start of `submitQuestion()`, before early branches, call `topicsFromQuestion(text)` and append the resulting topic-only Chat signal if nonempty. Do not pass this profile, topics, raw text, event IDs, history, or preferences to `/api/chat`; no provider/server behavior changes. Preserve the existing user-visible Chat turn handling and raw question persistence exactly as Phase 3 requires.</action>
  <verify><automated>npm run validate:curated && npm run validate:chat && npm run validate:recent && npm run build</automated></verify>
  <acceptance_criteria>Each signal is recorded only after its intended meaningful action, its ranking priority remains determined by the helper, and no disallowed behavioral data crosses the Curated key/server boundary.</acceptance_criteria>
  <done>Real Recent and Chat behavior now drives the local profile without weakening source, citation, or privacy constraints.</done>
</task>

</tasks>

<negative_prohibitions>
- Do not treat a row becoming selected by fallback, a citation click, scroll, elapsed duration samples, or a selected Chat citation as a Recent-read signal.
- Do not record failed copy attempts, clipboard text, raw Chat questions, entire thread history, model/provider output, URLs, quotes, scores, or action counts in Curated state.
- Do not call any API, import, model, vector/document search, server module, or account/profile service as part of signal capture.
- Do not reset or otherwise mutate `lamplighter-chat-v1` in this plan.
</negative_prohibitions>

<threat_model>
| Threat ID | Category | Severity | Mitigation |
|---|---|---:|---|
| T-04-06 | Information disclosure | high | Topic extraction returns only known normalized tags; Chat text never enters Curated state or API payload beyond the existing Chat request. |
| T-04-07 | Integrity | high | Recent timer begins only from explicit Recent UI and is cancelled at all scope/lifecycle boundaries; clipboard signals require success. |
| T-04-08 | Tampering | medium | State writes use the Plan 1 bounded validated shape and re-parse on load. |
| T-04-09 | Profiling | high | No duration/event log/count display, ideology classification, demographics, or server-side behavior tracking is added. |
</threat_model>

<verification>
Run `npm run validate:curated`, `npm run validate:chat`, `npm run validate:recent`, and `npm run build`. Manually: open Recent record for less than 10 seconds then switch record; hold another for 10 seconds; refresh; copy a citation with clipboard available and with it unavailable; submit a bundled, unsupported, and candidate-position Chat prompt; inspect browser storage to confirm only the Curated key contains tags/record IDs/timestamps and Chat history remains intact.
</verification>

<success_criteria>
- Curated receives only the locked hierarchy of minimal local signals from existing actions.
- Explicit Recent open timing and successful-copy rules prevent accidental activity capture.
- Chat behavior and server evidence boundaries remain untouched.
</success_criteria>

<output>
Create `.planning/phases/04-curated/04-02-SUMMARY.md` when done.
</output>
