---
phase: 04-curated
plan: "03"
type: execute
wave: 3
depends_on: ["04-01", "04-02"]
files_modified:
  - src/App.tsx
  - src/styles.css
autonomous: true
requirements:
  - CUR-01
  - CUR-02
  - CUR-03
  - CUR-04
must_haves:
  truths:
    - "The bottom Curated tab is a real selected view that renders every ranked candidate from the active coverage only; it reuses Recent rows and the shared evidence panel."
    - "A cold, invalid, or reset profile visibly returns the active coverage's newest-first evidence-backed starter view, rather than widening sources or fabricating relevance."
    - "Each ranked item has a deterministic Why this line naming only applicable civic topic, strongest approved action type, and public-record recency; it never characterizes the user or calls a model."
    - "Users can edit local civic topic chips/add-topic input and reset only Curated signals/topics through native confirmation; success returns to starter order while preserving Chat history."
  artifacts:
    - path: src/App.tsx
      provides: "Active Curated tab, ranked safe list, topic controls, local-only note, native-confirm reset, success status, and shared provenance selection"
      contains: "Why this"
    - path: src/styles.css
      provides: "Compact accessible Curated controls and explanation/reset styling in the existing editorial shell"
      contains: "curated"
  key_links:
    - from: "Curated row click"
      to: "existing selectedId/detailPanel"
      via: "same evidence selection path used by Recent and Chat citations"
      pattern: "setSelectedId"
    - from: "Curated reset"
      to: src/curated.ts
      via: "native confirm then replace only isolated Curated state with empty starter profile"
      pattern: "confirm"
---

<objective>
Turn the validated local ranking into the Curated screen and complete the personalized demo loop.

Purpose: give judges a concise, transparent personalized feed without a second evidence UI, behavioral dashboard, account surface, or opaque model explanation.
Output: a working Curated tab with ranked evidence-backed rows, local topic controls, plain explanations, and safe reset.
</objective>

<execution_context>
@/Users/ssongirk/.codex/gsd-core/workflows/execute-plan.md
@/Users/ssongirk/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/04-curated/04-CONTEXT.md
@.planning/phases/04-curated/04-RESEARCH.md
@.planning/phases/04-curated/04-PATTERNS.md
@.planning/phases/04-curated/04-01-SUMMARY.md
@.planning/phases/04-curated/04-02-SUMMARY.md
@src/App.tsx
@src/curated.ts
@src/styles.css
@src/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace the unavailable Curated navigation with the scoped ranked feed and shared provenance UI</name>
  <files>src/App.tsx</files>
  <read_first>src/App.tsx; src/curated.ts; src/types.ts; .planning/phases/04-curated/04-CONTEXT.md; .planning/phases/04-curated/04-PATTERNS.md</read_first>
  <action>Extend `ActiveTab` to include `curated`, change the Curated bottom-nav button into a normal active-tab control, and preserve Recent/Chat behavior. Use the already derived active-coverage `records` array as the only input to `rankCuratedRecords`. Render every returned record using the same evidence-backed row markup/source labels/date/quote/locator and the same `selectedId` detail-panel path; clicking it must not itself create a Recent-open signal. Update header/status copy to accurately say Curated is offline/local personalization over the selected coverage. When ranking reports no usable profile, clearly label the newest-first list as the selected coverage's starter view. When no eligible records exist, reuse the existing scope-honest empty-state posture: do not search, substitute reporting, broaden coverage, or fetch. Keep the Chat's closed packet and Recent ordering code intact.</action>
  <verify><automated>npm run validate:curated && npm run validate:recent && npm run validate:chat && npm run build</automated></verify>
  <acceptance_criteria>A user can move Recent → Curated → evidence panel and see only existing active-coverage records in a transparent deterministic order.</acceptance_criteria>
  <done>Curated is a real primary screen rather than an unavailable placeholder, with no second citation/provenance implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add compact topic controls, deterministic explanations, and reset isolation</name>
  <files>src/App.tsx, src/styles.css</files>
  <read_first>src/App.tsx; src/curated.ts; src/styles.css; .planning/phases/04-curated/04-CONTEXT.md; .planning/phases/04-curated/04-RESEARCH.md</read_first>
  <behavior>
    - Existing inferred/active topics and a small fixed civic vocabulary appear as selectable chips; one Add topic field creates a normalized local chip without a request.
    - Duplicate, blank, or invalid custom topic input is a no-op. Unknown manual topics remain honest preferences but do not fabricate matching updates.
    - Each record gets one local deterministic `Why this` line. It identifies matching topic, highest applicable source action (`recent Chat question`, `copied citation`, `opened update`), and recency; starter-only records say starter view instead.
    - The view says personalization stays in this browser and can be reset, but exposes no raw question, action count/history, timestamps, profile vector, or political characterization.
    - Reset uses `window.confirm`; on approval it clears/replaces only Curated state/topics, leaves local Chat threads untouched, returns immediately to starter ordering, and announces success in a `role=status` message.
  </behavior>
  <action>Inside Curated, render the Plan 1 helper's normalized topic controls: accessible toggle buttons/chips plus a labeled Add topic form/input. Update only the isolated Curated state through its helpers; do not create Account/settings navigation, a modal/toast library, or a generic preference system. Render the helper's explanation data as a concise `Why this` line below each row, naming topic/action/recency only when applicable; do not use GPT, prompt text, scores, counts, or inferred user traits. Add a short local-only privacy note and a semantic reset button. On reset, call native `window.confirm` with a clear scope message; only when confirmed replace Curated state with its empty state, clear topic input, and set a brief success `role=status` message. Never call `setThreads`, remove the Chat storage key, or clear visible Chat state. Add only the necessary custom CSS for chips, Add-topic control, why line, note/reset/status, 44px touch targets, focus visibility, responsive wrapping, and non-color state cues; reuse existing grid/list/panel styles rather than add a component library or redesign.</action>
  <verify><automated>npm run validate:curated && npm run validate:contract && npm run validate:seed && npm run validate:recent && npm run validate:chat && npm run validate:import && npm run build</automated></verify>
  <acceptance_criteria>Topic edits and reset are local, accessible, explainable, and cannot alter Chat history or introduce a source/model request.</acceptance_criteria>
  <done>The Curated experience makes its personalization rationale visible without becoming a behavioral profile or hiding evidence.</done>
</task>

</tasks>

<negative_prohibitions>
- Do not add an Account tab, account/auth, persistent server profile, source expansion control, full-document PDF/transcript RAG, Chat RAG, vector provider, vector DB, model-written explanation, or API request.
- Do not render source URLs/quotes/citations outside the existing detail panel, and do not fork/duplicate that panel.
- Do not hide candidates with an arbitrary top-N cap: render all eligible active-coverage records in ranked order.
- Do not turn topic chips, explanations, or reset status into an event-history/profiling dashboard.
</negative_prohibitions>

<threat_model>
| Threat ID | Category | Severity | Mitigation |
|---|---|---:|---|
| T-04-10 | Integrity | high | Curated uses existing coverage-derived rows and exact shared evidence selection; no retrieval/fetch path can inject new sources. |
| T-04-11 | Information disclosure | high | UI reveals only local note and per-item explanation, never raw questions, event history, timestamps, vectors, provider data, or Chat storage. |
| T-04-12 | Repudiation | medium | Deterministic `Why this` maps only to visible topic/action/recency inputs; no opaque model prose is presented as an explanation. |
| T-04-13 | Tampering | medium | Reset acts only on validated isolated Curated state after native confirmation; it does not touch Chat key/state. |
| T-04-14 | Accessibility denial | low | Native buttons/forms/confirmation, role=status, labels, keyboard focus, 44px targets, and responsive control wrapping preserve usability. |
</threat_model>

<verification>
Run the entire no-network suite: `npm run validate:contract && npm run validate:seed && npm run validate:recent && npm run validate:chat && npm run validate:curated && npm run validate:import && npm run build`. Manually record the demo path: pick Indianapolis; open a Recent row for 10 seconds; submit a supported Chat question; copy a citation; open Curated and verify housing/topic-related records rise with clear Why-this lines; add/remove a topic; click a Curated row and inspect the same source panel; reset, accept confirmation, observe starter view/success message; switch to Chat and refresh to verify its thread remains. Check narrow and desktop layouts plus keyboard focus.
</verification>

<success_criteria>
- Curated renders every eligible, evidence-backed update only for active configured coverage.
- Minimal local activity/topics influence deterministic local ordering and explain themselves without political profiling.
- Reset reliably returns starter order while preserving Chat history.
</success_criteria>

<output>
Create `.planning/phases/04-curated/04-03-SUMMARY.md` when done.
</output>
