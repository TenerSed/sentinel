---
phase: 04-curated
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - data/seed-records.json
  - scripts/seed.mjs
  - scripts/validate-seed.mjs
  - scripts/curated-check.mjs
  - package.json
  - src/types.ts
  - src/demo-seed.ts
  - src/curated.ts
autonomous: true
requirements:
  - CUR-01
  - CUR-02
  - CUR-03
  - CUR-04
must_haves:
  truths:
    - "Curated candidates can only be supplied by the caller from the active coverage's existing evidence-backed records; the helper cannot query the seed, SQLite, documents, or the network."
    - "Every committed browser record has normalized civic topics and one finite, fixed-size, L2-normalized precomputed embedding; malformed fixture metadata stops seed generation."
    - "Blank or invalid local Curated state produces the same newest-first candidate order as Recent, while a valid profile ranks deterministically with Chat topic signals stronger than citation signals, which are stronger than qualified Recent opens."
    - "The Curated storage contract contains topic tags, public record IDs, and timestamps only; it cannot serialize a raw Chat question, source text, provider/model data, scores, or Chat threads."
  artifacts:
    - path: src/curated.ts
      provides: "Browser-safe topic normalization, fail-closed Curated state parsing, array cosine ranking, deterministic explanation data, and isolated reset helpers"
      exports: ["CURATED_STORAGE_KEY", "parseCuratedState", "rankCuratedRecords", "recordSignal", "normalizeTopic", "topicsFromQuestion"]
    - path: scripts/curated-check.mjs
      provides: "No-network assert regression that transpiles/imports the production Curated helper for metadata, ranking, privacy, coverage candidates, and reset isolation"
      contains: "transpileModule"
    - path: data/seed-records.json
      provides: "Hand-reviewed per-update civic topics and precomputed embeddings for the bundled safe corpus"
      contains: "embedding"
  key_links:
    - from: data/seed-records.json
      to: src/demo-seed.ts
      via: "scripts/seed.mjs validates and projects only curated ranking metadata by existing record ID"
      pattern: "topics|embedding"
    - from: src/curated.ts
      to: src/App.tsx
      via: "App passes coverageRecords() output into the pure ranker rather than letting Curated broaden the corpus"
      pattern: "rankCuratedRecords"
---

<objective>
Build the offline Curated ranking contract before capturing user activity or rendering a new tab.

Purpose: make personalization inspectable, deterministic, and safe for the no-key demo while preserving the established evidence/coverage boundary.
Output: validated per-record ranking metadata, a small pure local helper, and an executable no-network regression check.
</objective>

<execution_context>
@/Users/ssongirk/.codex/gsd-core/workflows/execute-plan.md
@/Users/ssongirk/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/04-curated/04-CONTEXT.md
@.planning/phases/04-curated/04-RESEARCH.md
@.planning/phases/04-curated/04-PATTERNS.md
@data/seed-records.json
@scripts/seed.mjs
@scripts/validate-seed.mjs
@scripts/recent-scope-check.mjs
@src/types.ts
@src/demo-seed.ts
@src/App.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add verified, browser-safe curated metadata to the committed seed path</name>
  <files>data/seed-records.json, scripts/seed.mjs, scripts/validate-seed.mjs, src/types.ts, src/demo-seed.ts</files>
  <read_first>data/seed-records.json; scripts/seed.mjs; scripts/validate-seed.mjs; src/types.ts; src/demo-seed.ts; scripts/evidence-contract.mjs</read_first>
  <action>Add a compact `topics: string[]` and `embedding: number[]` to every fixture record. Hand-review a small shared civic vocabulary (for example housing, budget, environment, education, technology, government-operations) and fixed-dimensional vectors; use one dimension everywhere, finite numbers only, nonzero L2 norm, and L2-normalized vectors. This is committed demo metadata, not a runtime provider result. Extend `EvidenceRecord` with these browser-safe fields. In `validateFixture()`, fail on a missing/empty topic list, invalid lowercase trimmed/deduplicated topic ID, dimension mismatch, non-finite entry, zero norm, or non-normalized vector. In `writeDemoSeed()`, join fixture metadata to the existing projected record by ID and emit only topics/embedding alongside the already safe evidence fields; do not add a SQLite column/table/migration or expose raw text/hashes. Extend `assertSeedMatchesDatabase()` to compare generated metadata with the fixture while retaining SQLite as authority for evidence fields. Run `npm run seed` and commit regenerated `src/demo-seed.ts`.</action>
  <verify><automated>npm run seed && npm run validate:seed</automated></verify>
  <acceptance_criteria>Every browser record has validated curated metadata, the generated projection exactly matches the fixture metadata, and the committed SQLite contract is unchanged.</acceptance_criteria>
  <done>Curated can rank the safe existing update projection without an embedding API, vector DB, or document search.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the smallest pure, fail-closed local ranking and storage helper</name>
  <files>src/curated.ts, src/types.ts</files>
  <read_first>src/types.ts; src/App.tsx; src/demo-seed.ts; .planning/phases/04-curated/04-CONTEXT.md; .planning/phases/04-curated/04-RESEARCH.md</read_first>
  <behavior>
    - A Curated profile is versioned and contains only manually selected normalized topic strings plus valid `{ kind, recordId/topics, at }` signals.
    - `chat` signals contain normalized topic tags and timestamps, never question text; `citation` and `recent` signals contain only a current seed record ID and timestamp.
    - Invalid, foreign, malformed, obsolete, or oversized persisted values fail to an empty profile instead of being repaired, fetched, or rendered.
    - Ranking accepts an already scoped record array, never a seed or coverage ID; it uses local cosine and topic overlap only to reorder that input.
    - No usable profile returns a starter result in input order. A profile result is stable by score, publication date, then record ID and yields deterministic `Why this` ingredients only.
  </behavior>
  <action>Create a single browser-safe `src/curated.ts` module. Define the minimal `CuratedSignal`, `CuratedState`, ranked-record/explanation types in `src/types.ts` only if needed by App. Export a versioned storage-key constant, bounded/fail-closed parser, topic normalizer, transient known-topic extractor for submitted Chat text, signal append/deduplication helper, reset/empty-state helper, and `rankCuratedRecords(candidates, state)`. Use standard array loops and dot products for normalized-vector cosine; no dependency, `fetch`, provider call, server import, local database, or broad record lookup. Weight contributing action classes explicitly so Chat > citation > Recent; use a small public `publishedAt` recency contribution only for deterministic preference/tie behavior. Manual topics are explicit non-inferred preferences. Build the profile from record-backed signals plus matching topic tags, never ideology/demographic/political labels. Return explanation data naming at most a matching topic, strongest action label, and recency phrase; App will render it. Cap/deduplicate signals to keep browser storage bounded, but expose neither count nor event timeline. Add a `ponytail:` ceiling comment beside the intentional O(records × signals) ranking noting indexed/server retrieval only when corpus size warrants it.</action>
  <verify><automated>npm run build</automated></verify>
  <acceptance_criteria>The helper has one authority for local Curated state and ranking, cannot retain raw submitted text, and cannot widen candidates beyond its caller input.</acceptance_criteria>
  <done>The app has a deterministic, package-free personalization primitive ready for signal instrumentation and UI rendering.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Lock metadata, ranking, privacy, and reset invariants in one offline assertion check</name>
  <files>scripts/curated-check.mjs, package.json</files>
  <read_first>src/curated.ts; src/types.ts; src/demo-seed.ts; data/seed-records.json; scripts/seed.mjs; scripts/validate-seed.mjs; scripts/recent-scope-check.mjs; package.json</read_first>
  <action>Create `scripts/curated-check.mjs` with `node:assert/strict` and no browser automation, provider call, or network. Reuse the existing `scripts/chat-contract-check.mjs` pattern: read the real `src/curated.ts`, compile it in-process with `typescript.transpileModule` (`ModuleKind.ESNext`/current supported target), then import the emitted data URL and exercise its actual exported parser/ranker/signal/reset functions. Do not duplicate production ranking or storage logic in the check. Add `npm run validate:curated`. The check must prove: every fixture/generated record has matching valid topic/vector metadata; all Indianapolis, Indiana, and federal candidate arrays come from the same coverage projection behavior as `coverageRecords()` rather than seed-wide records; cosine/score ordering makes otherwise equivalent Chat-topic signals outrank citation, which outranks Recent; ties resolve deterministically by public date then ID; manual-topic and unknown-topic behavior never fabricates a candidate; blank/invalid state returns newest-first starter order; parser rejects raw-question-shaped Chat state, unknown record IDs, bad timestamps, bad versions, and oversized/invalid values; and reset produces empty signals/topics without mutating a supplied `lamplighter-chat-v1` object. Use small in-script test records where that is clearer than mutating seed fixtures. Do not read real environment variables. Add the check to the final verification command set but do not change existing checks.</action>
  <verify><automated>npm run validate:curated && npm run validate:contract && npm run validate:seed && npm run validate:recent && npm run validate:chat && npm run build</automated></verify>
  <acceptance_criteria>One deterministic command catches fixture, ranking-order, location-boundary, raw-question, and reset-isolation regressions without an API key or network.</acceptance_criteria>
  <done>The offline semantic-ranking implementation is demonstrably constrained before it receives live UI events.</done>
</task>

</tasks>

<negative_prohibitions>
- Do not add an embedding provider, vector index/database, SQLite migration, API route, server module, document/transcript retrieval, RAG, source expansion, Account tab, or dependency.
- Do not let `src/curated.ts` import `demoSeed`, SQLite, server code, environment values, or raw source text.
- Do not persist or derive an ideology, demographic, political label, raw Chat question, provider output, citation quote, URL, score history, or event timeline in Curated storage.
- Do not change the closed Chat evidence packet or existing Chat-thread storage contract.
</negative_prohibitions>

<threat_model>
| Threat ID | Category | Severity | Mitigation |
|---|---|---:|---|
| T-04-01 | Tampering | high | Fail-closed versioned parser accepts only current seed IDs, valid timestamps, permitted signal shapes, bounded normalized topics, and fixed metadata. |
| T-04-02 | Information disclosure | high | Curated state stores topic tags/timestamps and public record IDs only; raw question text and Chat threads remain outside its key. |
| T-04-03 | Integrity | high | Ranker receives only caller-scoped `coverageRecords()` candidates; no seed-wide/document/vector retrieval path exists. |
| T-04-04 | Profiling | high | Tags/actions are explicit local inputs; explanations never infer ideology, demographics, or political affinity. |
| T-04-05 | Denial of service | low | Bounded signals/topics and fixed tiny vectors keep local storage and array scoring finite. |
</threat_model>

<verification>
Run `npm run seed`, `npm run validate:seed`, `npm run validate:curated`, `npm run validate:contract`, `npm run validate:recent`, `npm run validate:chat`, and `npm run build`. Inspect the generated seed once to confirm vectors/topics are present but raw source fields remain absent.
</verification>

<success_criteria>
- Committed, validated per-update vectors support local cosine ranking of the existing evidence-backed corpus only.
- Local Curated state is privacy-minimal, fail-closed, and resettable without affecting Chat history.
- Ranking explanations have deterministic ingredients instead of model-written personalization claims.
</success_criteria>

<output>
Create `.planning/phases/04-curated/04-01-SUMMARY.md` when done.
</output>
