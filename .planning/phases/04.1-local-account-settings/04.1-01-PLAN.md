---
phase: 04.1-local-account-settings
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/App.tsx
  - src/styles.css
autonomous: true
requirements:
  - SET-01
  - SET-02
  - SET-03
must_haves:
  truths:
    - "A fourth keyboard-accessible Account tab exposes only local Lamplighter controls and never presents sign-in, account creation, sync, or a server profile."
    - "Account changes the existing active coverage, manages the same Curated topics/signals, and resets Curated without changing Chat history."
    - "Clearing Chat history is a separate native-confirm action that clears all local Chat threads without changing Curated state, topics, coverage, or provider behavior."
    - "Account accurately explains local browser handling and optional live-provider availability without showing keys, provider configuration, raw storage, event history, or political inferences."
  artifacts:
    - path: src/App.tsx
      provides: "Fourth Account navigation/view using existing local state, coverage action, Curated controls/reset, explicit Chat clear, and safe provider/privacy copy"
      contains: "Account"
    - path: src/styles.css
      provides: "Responsive accessible Account setting groups that reuse the editorial shell and existing button/control styles"
      contains: "account"
  key_links:
    - from: "Account coverage select"
      to: "existing selectCoverage"
      via: "the existing configured location list and active coverage state"
      pattern: "selectCoverage"
    - from: "Account Curated controls"
      to: "existing curated state/resetCurated"
      via: "the same isolated lamplighter-curated-v1 lifecycle already used by Curated"
      pattern: "resetCurated"
    - from: "Clear local Chat history"
      to: "existing threads and lamplighter-chat-v1"
      via: "native confirmation followed by local thread/key clearing only"
      pattern: "setThreads"
---

<objective>
Add one local-only Account tab that exposes existing preferences safely, without broadening data, retrieval, or provider behavior.

Purpose: make the demo's local controls easy to find and make the no-account privacy boundary explicit.
Output: a fourth responsive Account screen that reuses current coverage, Curated, and Chat state/actions.
</objective>

<execution_context>
@/Users/ssongirk/.codex/gsd-core/workflows/execute-plan.md
@/Users/ssongirk/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.1-local-account-settings/04.1-CONTEXT.md
@.planning/phases/04-curated/04-CONTEXT.md
@.planning/phases/04-curated/04-03-SUMMARY.md
@src/App.tsx
@src/curated.ts
@src/types.ts
@src/styles.css
@scripts/curated-check.mjs
@scripts/chat-contract-check.mjs
@package.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Turn the existing local controls into a fourth Account view</name>
  <files>src/App.tsx</files>
  <read_first>src/App.tsx; src/curated.ts; src/types.ts; .planning/phases/04.1-local-account-settings/04.1-CONTEXT.md; .planning/phases/04-curated/04-03-SUMMARY.md</read_first>
  <behavior>
    - Add `account` to the existing `ActiveTab` union and a fourth normal bottom-nav button with the existing `aria-current="page"` active-state convention.
    - The Account view is built only after the bundled seed is ready and uses the existing `state.seed.locations`, `coverageId`, and `selectCoverage` action; it cannot invent or widen configured coverage.
    - Account reuses the existing Curated topic chips/add-topic form and `resetCurated` behavior/state rather than adding a preference store, duplicate storage key, account model, or API request. Curated reset continues to preserve Chat history.
    - Account has a separately labeled destructive Chat-history action. Only accepted `window.confirm` clears all existing local `threads` and the existing Chat storage key; cancellation does nothing. It must not call `setCurated`, reset coverage, alter provider availability, or change the Curated key.
    - A concise `role="status"` result follows a successful clear. Curated's existing reset status remains isolated and truthful.
    - The Account privacy/status sections say that coverage, Curated preferences/signals, and Chat history are browser-local for this demo; there is no sign-in, sync, or server profile. They must also accurately state whether optional live Chat is available, while neither exposing nor accepting provider keys/configuration/model controls, raw local-storage values, activity history/counts, vectors, timestamps, or political labels.
  </behavior>
  <action>Keep this in `App.tsx`; do not introduce a settings component, context, route, API, database table, dependency, or new browser-storage schema. Extend the existing tab/header branching with a concise Account title/status. Reuse the existing coverage selector markup and `selectCoverage` in Account (hide the duplicate header selector while Account is active if needed, rather than maintaining two competing controls). Factor the already-present Curated topic controls into one local JSX value/render path if that is the smallest way to render the same controls in Curated and Account; preserve their labels, 44px targets, native form behavior, validation, and isolated `curated` state. Reuse `resetCurated()` unchanged for both places so its confirmation explicitly preserves Chat. Add a `clearChatHistory()` event handler next to it: native-confirm that this removes local Chat history only, then replace `threads` with `{}`, remove the existing `lamplighter-chat-v1` key defensively, and set an Account-only success message. Do not change `validStoredThreads`, Chat answer persistence, `/api/chat`, `/api/chat/status`, provider fallback, source/evidence selection, ranking, or local Curated parsing. Keep the global coverage behavior intact for Recent/Curated/Chat and use the current `live.available` boolean only for an honest availability label.</action>
  <verify><automated>npm run validate:curated && npm run validate:chat && npm run validate:recent && npm run build</automated></verify>
  <acceptance_criteria>Account can select each already-configured coverage, toggle/add Curated topics, reset Curated while Chat remains visible after switching tabs/refresh, and separately clear Chat only after a native confirmation.</acceptance_criteria>
  <done>A fourth Account tab consolidates the existing local settings without creating an account system or a new data boundary.</done>
</task>

<task type="auto">
  <name>Task 2: Style the Account groups without creating a second design system</name>
  <files>src/styles.css</files>
  <read_first>src/styles.css; src/App.tsx; .planning/phases/04.1-local-account-settings/04.1-CONTEXT.md</read_first>
  <action>Add only the CSS needed for an Account panel/group layout within the existing `.recent-shell` and editorial card treatment: section spacing, concise local/privacy/provider copy, an explicit destructive Chat-clear button, status line, and the four-column bottom navigation. Reuse existing `.coverage-control`, `.topic-chips`, `.topic-form`, `.topic-chip`, `.reset-curated`, focus-visible rule, and mobile breakpoint where possible. Ensure native selects/buttons/forms remain keyboard usable, all actions retain at least 44px touch targets, the active Account nav is not color-only, and the tab labels remain readable at the existing narrow breakpoint. Do not add icons, animations, a modal/toast library, account avatar, dashboard/event timeline, or a new color system.</action>
  <verify><automated>npm run validate:contract && npm run validate:seed && npm run validate:recent && npm run validate:chat && npm run validate:curated && npm run validate:import && npm run build</automated></verify>
  <acceptance_criteria>The Account screen reads as a calm extension of the existing app at desktop and narrow widths, and all settings actions remain visible, labeled, focused, and usable with a keyboard.</acceptance_criteria>
  <done>The new local controls fit the existing UI without new visual infrastructure.</done>
</task>

</tasks>

<negative_prohibitions>
- Do not add authentication, account creation, identity, sync, cloud persistence, server-side profile/preferences, database changes, or a preference API.
- Do not add full-document PDF/transcript RAG, source search/expansion, retrieval changes, provider memory, provider configuration, embedding changes, model controls, or any dependency.
- Do not show API keys, environment names/values, raw localStorage payloads, raw Chat question history in a settings summary, action history/counts/timestamps, profile vectors, inferred ideology, or demographic/political labels.
- Do not make Curated reset clear Chat, and do not make Chat clearing reset Curated state/topics or coverage.
</negative_prohibitions>

<threat_model>
| Threat ID | Category | Severity | Mitigation |
|---|---|---:|---|
| T-04.1-01 | Information disclosure | high | Account shows only plain local-storage/privacy boundaries and a boolean provider availability label; no secret, raw state, history, provider configuration, or profile data is rendered. |
| T-04.1-02 | Integrity | high | Coverage continues through the existing configured registry/action; Account adds no source, retrieval, or provider path. |
| T-04.1-03 | Tampering | medium | Native confirmations and isolated state actions make Curated reset and Chat clearing explicit, with each action touching only its own existing local key/state. |
| T-04.1-04 | Privacy | high | Reuse bounded Curated storage and existing browser-local Chat threads; do not add telemetry, activity analytics, sync, or server preference persistence. |
| T-04.1-05 | Accessibility | medium | Native controls, visible labels, `aria-current`, focus styles, `role=status`, touch targets, and responsive navigation preserve keyboard and narrow-screen use. |
</threat_model>

<verification>
Run the complete no-network suite: `npm run validate:contract && npm run validate:seed && npm run validate:recent && npm run validate:chat && npm run validate:curated && npm run validate:import && npm run build`.

Manually: use keyboard navigation to open Account and confirm its fourth nav state; switch coverage to Indianapolis, Indiana, and U.S. federal and verify Recent/Curated/Chat retain the existing scoped records; add/remove a Curated topic in Account and see the same state in Curated; reset Curated, accept confirmation, then verify Chat threads remain after switching and refreshing; create a bundled Chat turn, cancel Account's Clear Chat confirmation, then accept it and verify all Chat coverage threads are gone while Curated topics/signals and coverage remain. Check Account at 320px and desktop widths. With no configured live provider, verify the label says unavailable without exposing configuration; when a server status makes it available, verify only the availability wording changes.
</verification>

<success_criteria>
- Account is a real fourth local settings tab with no auth or backend profile.
- Existing coverage and Curated controls are reused, and reset isolation is preserved in both directions.
- Local privacy and optional provider status are clear without adding secrets, tracking, retrieval, or scope expansion.
</success_criteria>

<output>
Create `.planning/phases/04.1-local-account-settings/04.1-01-SUMMARY.md` when done.
</output>
