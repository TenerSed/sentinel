---
phase: 03-grounded-chat
plan: "03"
type: execute
wave: 3
depends_on: ["03-01", "03-02"]
files_modified:
  - src/App.tsx
  - src/styles.css
autonomous: true
requirements:
  - ASK-01
  - ASK-04
  - ASK-05
must_haves:
  truths:
    - "Chat is a real bottom-nav screen using the current coverage selector; switching coverage shows a separate local thread and never mixes evidence scopes."
    - "The no-key screen says Live AI unavailable while five or more suggested bundled questions remain usable and visibly labeled Bundled demo answer."
    - "Every displayed factual sentence/bullet AnswerBlock has buttons for IDs validated against the exact deterministic active-coverage `records.slice(0, 6)` packet; selecting one opens the existing shared evidence panel with stored quote, locator, and canonical URL."
    - "Unsupported, invalid, unavailable, or candidate-position-without-tag answers use the exact insufficient-evidence wording, show suggestions, and offer a non-fetching Find more sources explanation."
    - "Successful live answers disclose the actual provider, selected coverage, and packet record count; failure UI exposes only provider name and compact error type."
  artifacts:
    - path: src/App.tsx
      provides: "Location-scoped Chat view, local threads, preset/free-text flow, citation chips, and shared evidence panel integration"
      min_lines: 360
    - path: src/styles.css
      provides: "Responsive accessible Chat, answer, citation-chip, and status styling within existing editorial shell"
      min_lines: 380
  key_links:
    - from: src/App.tsx
      to: /api/chat
      via: "free-text request submits coverageId/question only and renders only validated response envelope"
      pattern: "coverageId|question"
    - from: src/App.tsx
      to: src/demo-chat.ts
      via: "no-key suggested prompts resolve deterministic packet-valid bundled answers"
      pattern: "Bundled demo answer"
    - from: src/App.tsx
      to: "existing selectedId detail panel"
      via: "citation chips select stored evidence record instead of provider-generated links"
      pattern: "setSelectedId"
---

<objective>
Replace the unavailable Chat tab with the grounded, location-scoped conversation experience.

Purpose: complete the demo path Recent → Chat → cited answer → existing evidence panel without a mock, silent source expansion, or loss of offline behavior.
Output: an accessible Chat screen with local per-coverage history, deterministic demos, optional live requests, and citation chips.
</objective>

<execution_context>
@/Users/ssongirk/.codex/gsd-core/workflows/execute-plan.md
@/Users/ssongirk/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/03-grounded-chat/03-CONTEXT.md
@.planning/phases/03-grounded-chat/03-AI-SPEC.md
@.planning/phases/03-grounded-chat/03-PATTERNS.md
@.planning/phases/03-grounded-chat/03-01-SUMMARY.md
@.planning/phases/03-grounded-chat/03-02-SUMMARY.md
@src/App.tsx
@src/styles.css
@src/types.ts
@src/demo-seed.ts
@src/demo-chat.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement the location-scoped Chat interaction and safe local threads</name>
  <files>src/App.tsx</files>
  <read_first>src/App.tsx; src/types.ts; src/demo-seed.ts; src/demo-chat.ts; .planning/phases/03-grounded-chat/03-CONTEXT.md; .planning/phases/03-grounded-chat/03-PATTERNS.md</read_first>
  <behavior>
    - Recent remains intact and the bottom Chat button switches to a real Chat screen; Curated remains an explicit unavailable future tab.
    - The existing coverage selector remains authoritative in the UI. Changing it displays its own thread, suggestions, and selected-record scope while retaining other coverage threads in versioned local storage.
    - Local storage is parsed defensively; malformed/outdated data is dropped rather than rendered. Stored turns carry only safe rendered fields and never alter the current evidence packet or candidate-position tag rule.
    - Five location-aware suggestions and a free-text composer are present. Presets resolve deterministic fixtures in no-key mode; free text uses the server only when status says a live provider is available.
  </behavior>
  <action>Refactor only enough of `App.tsx` to introduce an `activeTab` state while retaining existing Recent row/filter/evidence behavior and `selectedId` detail state. Add a Chat screen that uses the already selected coverage ID and eligible record order for display, but treats server response as the authority for live answers. Derive the exact deterministic active Chat packet as `records.slice(0, 6)` and use that same set—not all current coverage records—for preset lookup, response ID validation, citation-chip rendering, local-turn revalidation, and candidate-position tag lookup. On mount, call the safe status endpoint; show `Live AI unavailable` when none is configured while leaving bundled suggestions active. Use the same limited candidate/office-holder-position wording classifier as the server only as a UX optimization: if it recognizes a position request and the active packet has no `recent_public_position` tag, immediately append the fixed insufficient-evidence turn with suggestions and make no live request; the server preflight remains authoritative for every request. Resolve other presets through `demo-chat.ts` first, append a safe locally stored turn after validating its referenced IDs against the active six-record packet, and use `fetch('/api/chat')` only for non-preset questions when live status is available. Send exactly `{ coverageId, question }`; do not send record data, IDs, sources, local history, provider choice, or URLs. Validate the returned answer envelope/IDs and one-factual-sentence-or-bullet rule again on the client before rendering; anything malformed renders the fixed insufficient-evidence state instead of raw server/model text. Persist `lamplighter-chat-v1` as a coverage-ID-keyed thread map via a guarded localStorage read/write. Keep all prior coverage threads visible only when that coverage is selected; do not clear them on selector/tab changes. Add a concise loading state for the request and no product interaction/question limiter; Phase 3 limits live answer length only. Do not add search, importer calls, source substitution, autonomous agent behavior, or a fake successful answer.</action>
  <verify><automated>npm run validate:chat && npm run validate:seed && npm run validate:recent && npm run build</automated></verify>
  <acceptance_criteria>Chat is usable without a key through truthful presets, and optional live requests obey the exact selected coverage and server-only evidence boundary.</acceptance_criteria>
  <done>The active coverage selector drives both Recent and isolated Chat threads without any browser authority over sources or citations.</done>
</task>

<task type="auto">
  <name>Task 2: Render cited answers, refusal/failure states, and the shared provenance panel</name>
  <files>src/App.tsx, src/styles.css</files>
  <read_first>src/App.tsx; src/styles.css; src/types.ts; .planning/phases/03-grounded-chat/03-CONTEXT.md; .planning/phases/03-grounded-chat/03-AI-SPEC.md</read_first>
  <action>Render each validated AnswerBlock as exactly one factual sentence or bullet: first block is the short direct answer and subsequent blocks are bullets. Immediately after every block render labeled `<button>` citation chips for its validated active-packet IDs; clicking one calls the existing evidence selection path so the same detail panel displays its stored source quote, locator, reporting label, copy action, and unchanged canonical source URL. Never render a provider citation URL, title, locator, Markdown marker, or HTML. Display `Bundled demo answer` for fixtures; for live success show the actual returned provider and `Based only on {N} surfaced records for {location}.` For a provider outage show only the failed provider name plus compact error type; do not reveal raw diagnostics. Render the exact insufficient phrase `I can’t answer that from the selected public records yet,` followed by relevant current-coverage suggestions. Below it render `Find more sources` as a clearly future-scoped explanatory control: it may reveal static explanatory copy but must not call fetch, import, mutate seed, change coverage, or display reporting as a substitute. When supported sources materially differ, render the separately cited differing-claim blocks returned under the provider instruction and never select a winner or add interpretation. Add minimal stylesheet rules in the existing custom CSS: readable thread rhythm, 44px composer/actions/chips, semantic text labels, visible focus outline, status/error distinction beyond color, responsive evidence panel continuity, and bottom padding. Do not add a component/UI/icon/toast library, conversation bubbles that obscure citations, or a second source modal.</action>
  <verify><automated>npm run validate:chat && npm run build</automated></verify>
  <acceptance_criteria>Every displayed factual block carries an accessible citation path to the existing evidence panel; refusal, unavailable, and provider-failure states are honest and cannot trigger scope expansion.</acceptance_criteria>
  <done>A judge can see a supported cited answer, inspect its primary/reporting source, and understand when Lamplighter cannot answer.</done>
</task>

</tasks>

<negative_prohibitions>
- Do not remove/regress Recent, its location scope, or its existing evidence-panel canonical URL behavior.
- Do not render unvalidated API/model text or accept citation URLs/locators from any response.
- Do not silently search, ingest, broaden coverage, substitute reporting, persist raw provider data, or send local conversation history to a provider.
- Do not make Curated look implemented; it remains a clearly unavailable Phase 4 tab.
</negative_prohibitions>

<threat_model>
| Threat ID | Category | Severity | Mitigation |
|---|---|---:|---|
| T-03-11 | Tampering | high | Client revalidates response IDs and one-sentence/bullet blocks against the exact active `records.slice(0, 6)` packet and only sends coverageId/question. |
| T-03-12 | Spoofing | high | Citation chip maps ID to existing selected evidence panel; no provider-generated links reach DOM. |
| T-03-13 | Information disclosure | medium | Local storage contains safe rendered turns only; UI never shows raw errors, keys, prompts, or records outside the view. |
| T-03-14 | Integrity | high | Fixed refusal, client UX preflight, authoritative server tag preflight, and no-op source-expansion affordance prevent inferred candidate positions or silent source substitution. |
| T-03-15 | Accessibility denial | low | Semantic buttons/forms, focus outlines, labels, 44px targets, and responsive panel continuity. |
</threat_model>

<source_audit>
Manually walk every bundled prompt and citation chip against the stored record panel. For any live response used in recording, inspect every rendered block/citation and confirm its source kind is correctly labeled before filming.
</source_audit>

<verification>
Run `npm run validate:chat`, `npm run validate:contract`, `npm run validate:seed`, `npm run validate:recent`, `npm run validate:import`, and `npm run build`. In a no-key browser session: Recent → Chat → choose each coverage → answer a preset → click every citation → switch coverage → refresh and confirm only the matching stored thread appears. Ask an unsupported candidate-position question, an arbitrary question, and click Find more sources; each must refuse without a network/import request. Test narrow and desktop widths plus keyboard navigation.
</verification>

<success_criteria>
- Users can ask selected-coverage civic questions through deterministic demos or optional live Chat.
- Every supported answer block opens the same stored-evidence panel used by Recent.
- Unsupported, malformed, uncited, unavailable, or cross-coverage paths fail visibly and safely.
</success_criteria>

## Artifacts this plan produces

- The grounded Chat bottom-nav screen and local per-coverage threads.
- Accessible citation chips wired to the existing provenance panel.
- Honest bundled/live/unavailable/refusal states for the screen-recording demo.

<output>
Create `.planning/phases/03-grounded-chat/03-03-SUMMARY.md` when done.
</output>
