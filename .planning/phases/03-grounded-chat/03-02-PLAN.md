---
phase: 03-grounded-chat
plan: "02"
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/types.ts
  - src/demo-chat.ts
  - scripts/chat-contract-check.mjs
autonomous: true
requirements:
  - ASK-01
  - ASK-03
  - ASK-05
must_haves:
  truths:
    - "Five or more location-aware bundled demo questions return only pre-authored, packet-valid, one-factual-sentence-or-bullet answer blocks and are visibly distinguishable from live output."
    - "A question outside the current packet, including a candidate-position request without an explicit recent public statement, returns the fixed insufficient-evidence result."
    - "Browser-safe chat/data types retain the trusted evidence-kind tag while answer blocks express text and evidence IDs only; citation URLs, model text, provider credentials, and raw evidence bodies remain out of the chat fixture and local storage."
  artifacts:
    - path: src/types.ts
      provides: "Shared browser evidence-kind, answer, provider-status, and local thread contracts"
      exports: ["EvidenceKind", "GroundedAnswer", "AnswerBlock", "ChatTurn", "ChatProviderStatus"]
    - path: src/demo-chat.ts
      provides: "Five-plus deterministic bundled questions and packet-valid answer fixtures"
      contains: "Bundled demo answer"
    - path: scripts/chat-contract-check.mjs
      provides: "Preset answer assertions against real selected-location packets"
      contains: "preset"
  key_links:
    - from: src/demo-chat.ts
      to: src/demo-seed.ts
      via: "preset citations reference only stable existing evidence record IDs"
      pattern: "evidenceIds"
    - from: scripts/chat-contract-check.mjs
      to: src/demo-chat.ts
      via: "no-network validation checks every preset against its intended packet"
      pattern: "Bundled demo answer|preset"
---

<objective>
Add the deterministic, browser-safe answer fixtures and types that make Chat a reliable no-key demo.

Purpose: keep the demo useful when no provider is configured, while exercising exactly the same citation-block contract as the server path.
Output: a small preset library and type contract validated against the existing seeded records.
</objective>

<execution_context>
@/Users/ssongirk/.codex/gsd-core/workflows/execute-plan.md
@/Users/ssongirk/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/03-grounded-chat/03-CONTEXT.md
@.planning/phases/03-grounded-chat/03-AI-SPEC.md
@.planning/phases/03-grounded-chat/03-PATTERNS.md
@src/types.ts
@src/demo-seed.ts
@server/chat.mjs
@scripts/chat-contract-check.mjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the browser-safe grounded-answer and local-thread contracts</name>
  <files>src/types.ts</files>
  <read_first>src/types.ts; server/chat.mjs; .planning/phases/03-grounded-chat/03-AI-SPEC.md; .planning/phases/03-grounded-chat/03-CONTEXT.md</read_first>
  <action>Add the smallest shared chat types needed by React while preserving the `EvidenceKind`/`EvidenceRecord` field already established by Plan 03-01: a `GroundedAnswer` discriminated union (`answered` with 1–4 one-factual-sentence-or-bullet `AnswerBlock`s or `insufficient` with no blocks), a provider/status label union, and serializable local `ChatTurn`/per-coverage thread shapes. Make rendered citations record IDs only; do not add URL/locator/title fields to answer blocks. Keep local persisted history limited to user question, safe answer envelope, coverage ID, provider/bundled label, packet count, and timestamp—never provider raw output, browser evidence bodies, request diagnostics, API keys, or a model-selected source.</action>
  <verify><automated>npm run build</automated></verify>
  <acceptance_criteria>The client has one typed rendering contract for deterministic and live answers without becoming an authority over evidence or provider selection.</acceptance_criteria>
  <done>Chat UI can render validated server results and deterministic fixtures through one narrow, safe shape.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Author packet-bound bundled demo questions and answers</name>
  <files>src/demo-chat.ts, scripts/chat-contract-check.mjs</files>
  <read_first>src/demo-seed.ts; src/types.ts; server/chat.mjs; scripts/chat-contract-check.mjs; .planning/phases/03-grounded-chat/03-CONTEXT.md</read_first>
  <behavior>
    - At least five suggested questions exist across Indianapolis, Indiana, and U.S. federal coverage; they cover legislation, policy, and office-holder/public-record topics that the actual seed directly supports.
    - Every fixture answer uses the same answer blocks as a live answer, with one or more exact existing IDs after each claim/block; its visible source label is exactly `Bundled demo answer`.
    - Question matching is normalized, location-aware, and exact enough to remain deterministic. An arbitrary/off-topic question has no fixture and must refuse rather than approximate-match.
  </behavior>
  <action>Create `src/demo-chat.ts` beside the generated browser seed. Export location-aware suggested prompts plus a minimal normalizer/lookup for the pre-authored fixtures. Use five or more distinct preset prompts and only record IDs from the exact deterministic current-coverage `records.slice(0, 6)` packet; each AnswerBlock must be exactly one factual sentence or bullet, with optional separately cited detail blocks. Write only claims directly supported by the existing short stored quotes; label reporting as reporting and never create a claimed source disagreement unless the seed explicitly supports both claims. For candidate/office-holder position wording, return no bundled answer unless the exact client packet has an allowlisted `recent_public_position` record; the current seed may deliberately demonstrate the fixed refusal instead. Never infer position from vote, party, affiliation, reporting, or untagged office-holder records. Extend the chat contract check to load/derive each fixture’s exact client-equivalent packet and prove every fixture answer passes the same answer validator, its location is correct, a candidate-position query without a tag refuses, and an arbitrary normalized question returns no bundled result. Keep fixtures static and no-network; do not embed provider names, citations URLs, or source text copied beyond IDs and answer text.</action>
  <verify><automated>npm run validate:chat && npm run build</automated></verify>
  <acceptance_criteria>The no-key preset path reliably demonstrates cited answers across supported civic topics, and unsupported questions cannot accidentally become a bundled answer.</acceptance_criteria>
  <done>The offline demo has a source-honest Chat walkthrough independent of provider availability.</done>
</task>

</tasks>

<negative_prohibitions>
- Do not make fixtures look generated by GPT-5.6, Anthropic, or Gemini; label them `Bundled demo answer`.
- Do not add a generic local rule-based answerer, fuzzy/off-topic matching, or inferred candidate positions.
- Do not duplicate source URLs, locators, quotes, raw records, provider diagnostics, or API configuration into local storage/fixtures.
</negative_prohibitions>

<threat_model>
| Threat ID | Category | Severity | Mitigation |
|---|---|---:|---|
| T-03-07 | Spoofing | high | Preset IDs pass the same exact `records.slice(0, 6)` packet validator as live responses; bundled label cannot claim a provider. |
| T-03-08 | Tampering | medium | Match by normalized question plus coverage ID and validate browser storage before later rendering. |
| T-03-09 | Information disclosure | medium | Persist only safe displayed turns, not raw evidence/model/provider payloads. |
| T-03-10 | Integrity | high | Candidate-position questions have no preset and use the fixed refusal unless the exact packet carries a verified `recent_public_position` tag. |
</threat_model>

<source_audit>
Every answer block must be manually compared with the stored quote, locator, and source kind before recording. The source of truth remains the selected packet in `data/lamplighter.db`, not the fixture prose.
</source_audit>

<verification>
Run `npm run validate:chat` and `npm run build`. Manually read each five-plus fixture beside its selected seed record, confirming every displayed claim is directly supported and each citation ID is current-packet valid.
</verification>

<success_criteria>
- The no-key path has five or more truthful, location-aware cited examples.
- Bundled and live results share the same typed citation contract.
- No fixture allows a non-evidenced question or inferred political position.
</success_criteria>

## Artifacts this plan produces

- A browser-safe common grounded-answer/thread type contract.
- A deterministic preset catalog for the judge walkthrough.
- Expanded offline regression coverage for all presets.

<output>
Create `.planning/phases/03-grounded-chat/03-02-SUMMARY.md` when done.
</output>
